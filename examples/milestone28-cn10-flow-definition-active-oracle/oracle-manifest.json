{
  "schemaVersion": 1,
  "fixtures": [
    {
      "name": "flow-definition-active-fidelity",
      "project": ".",
      "entrypoint": {"kind": "invoke", "target": "M28CN10FlowDefinitionActiveOracle.run"},
      "compare": ["compile", "values"]
    }
  ]
}
